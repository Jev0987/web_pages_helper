# Chrome 标签页统一管理插件 原型说明文档

## 1. 文档目的

本文档用于说明 Chrome 标签页统一管理插件的页面原型、模块结构、核心交互和关键状态，作为产品、设计和开发协作依据。

## 2. 原型范围

本期原型聚焦以下页面与模块：

- 插件弹窗页 `Popup`
- 标签页完整管理页 `Manager Page`
- 分类管理弹层 `Category Modal`
- 标签编辑抽屉 `Edit Drawer`
- 批量关闭确认弹层 `Confirm Modal`

## 3. 设计目标

- 让用户在最短路径内完成标签页查看、分类、关闭和编辑
- 将“快速操作”和“深度管理”分层，避免弹窗过重
- 兼顾单标签操作和多标签批量操作
- 保持信息密度高，但不牺牲可读性

## 4. 页面结构总览

## 4.1 页面地图

1. Popup：快速查看与快捷操作
2. Manager Page：完整管理与批量整理
3. Category Modal：新建/编辑分类
4. Edit Drawer：编辑标签页自定义信息
5. Confirm Modal：高风险操作确认

## 5. Popup 原型说明

## 5.1 使用目标

用于用户快速打开插件并完成轻量操作：

- 查看当前窗口标签页摘要
- 查看分类概览
- 快速搜索标签页
- 快速关闭单个标签页
- 快速跳转到完整管理页

## 5.2 页面结构

### 顶部区域

- 产品名称/图标
- 搜索输入框
- “打开管理页”按钮

### 中部区域

- 分类摘要卡片区
- 当前窗口标签页列表

### 底部区域

- “重新自动分类”按钮
- “关闭当前分类冗余页”快捷入口

## 5.3 模块说明

### 模块 A：搜索框

功能：

- 支持按标题、URL、自定义名称搜索
- 输入后实时筛选当前窗口标签页

交互：

- 默认 placeholder：`搜索标签页 / URL / 备注`
- 输入后列表即时更新
- 清空输入恢复默认列表

### 模块 B：分类摘要卡片

展示内容：

- 分类名称
- 分类下标签数量
- 颜色标识

交互：

- 点击卡片后筛选当前分类标签页
- 再次点击取消筛选

### 模块 C：标签页列表

每项展示：

- 网站 favicon
- 显示标题
- 原始域名
- 所属分类标签
- 操作按钮

每项操作：

- 点击主体区域：切换到对应标签页
- 编辑按钮：打开编辑抽屉或跳转管理页详情态
- 关闭按钮：关闭该标签页

### 模块 D：快捷按钮

- 重新自动分类
- 打开完整管理页

## 5.4 Popup 状态

- 默认态：展示当前窗口全部标签页
- 搜索态：仅展示命中结果
- 分类筛选态：展示某一分类结果
- 空状态：无标签页或无搜索结果
- 加载态：初次读取标签页数据

## 6. Manager Page 原型说明

## 6.1 使用目标

用于完整管理所有标签页，支持分类维护、批量操作、编辑和跨窗口查看。

## 6.2 页面布局

建议使用三栏布局。

### 左侧栏：分类区

- 全部标签页
- 未分类
- 系统默认分类
- 用户自定义分类
- 新建分类按钮

### 中间主区域：标签页列表区

- 顶部工具栏
- 标签页列表
- 批量操作条

### 右侧栏：详情编辑区

- 标签页详细信息
- 自定义标题编辑
- 备注编辑
- 分类调整

## 6.3 左侧分类区说明

展示内容：

- 分类名称
- 数量
- 来源标记：自动 / 手动

交互：

- 点击分类后，中间区域仅展示该分类标签页
- 鼠标悬停显示更多操作：
  - 编辑分类
  - 删除分类

特殊项：

- `全部标签页`
- `未分类`
- `最近关闭`，如后续支持

## 6.4 顶部工具栏说明

包含以下控件：

- 全局搜索框
- 窗口筛选器
- 排序器
- 重新分类按钮
- 批量选择开关

排序方式建议：

- 最近访问
- 创建时间
- 标题字母顺序
- 域名分组

## 6.5 标签页列表区说明

建议采用列表卡片形式，每行一条。

每条展示信息：

- 多选框
- favicon
- 自定义显示标题 / 原始标题
- URL 或域名
- 所属窗口标识
- 所属分类
- 备注摘要
- 操作按钮

单条操作：

- 打开
- 编辑
- 移动分类
- 关闭

支持交互：

- 拖拽到左侧分类中完成归类
- 行点击进入右侧详情
- 多选后触发批量操作

## 6.6 批量操作条说明

当用户勾选一个或多个标签页后显示。

支持操作：

- 移动到分类
- 批量关闭
- 批量取消分类
- 批量重新分类

显示内容：

- 已选数量
- 当前可执行操作按钮

## 6.7 右侧详情编辑区说明

字段如下：

- 原始标题
- 当前 URL
- 自定义显示名称
- 备注
- 当前分类
- 是否收藏/重点标记

操作：

- 保存
- 取消
- 打开标签页
- 关闭标签页

交互：

- 编辑后即时保存或点击保存提交
- 保存成功后提示 toast

## 7. 分类管理弹层说明

## 7.1 使用场景

- 新建分类
- 编辑分类名称
- 删除分类前确认

## 7.2 字段设计

- 分类名称
- 分类颜色
- 分类来源说明（仅展示）

## 7.3 校验规则

- 分类名称不能为空
- 分类名称长度建议不超过 20 字
- 同级分类名称不可重复

## 8. 标签编辑抽屉说明

## 8.1 使用场景

- 编辑单个标签页元信息

## 8.2 字段

- 自定义标题
- 备注
- 分类

## 8.3 行为

- 默认带出当前值
- 可取消
- 可保存
- 若内容未变更，保存按钮置灰

## 9. 确认弹层说明

适用于以下操作：

- 批量关闭标签页
- 删除分类

展示内容：

- 操作对象数量
- 风险提示
- 确认按钮
- 取消按钮

建议：

- 批量关闭时增加“本次不再提示”开关

## 10. 关键交互流程

## 10.1 自动分类流程

1. 用户点击插件图标
2. Popup 加载当前窗口标签页
3. 系统按规则自动分类
4. 分类摘要卡片更新
5. 用户进入管理页查看结果

## 10.2 手动分类流程

1. 用户进入管理页
2. 选择标签页
3. 点击“移动到分类”或拖拽到左侧分类
4. 系统保存分类关系
5. 标签页列表实时刷新

## 10.3 编辑标签信息流程

1. 用户点击标签页“编辑”
2. 打开编辑抽屉
3. 修改自定义标题或备注
4. 点击保存
5. 列表展示更新后的自定义信息

## 10.4 批量关闭流程

1. 用户勾选多个标签页
2. 批量操作条出现
3. 用户点击“批量关闭”
4. 弹出确认框
5. 用户确认后关闭
6. 列表刷新并提示结果

## 11. 空状态与异常状态

## 11.1 空状态

- 当前无标签页
- 当前分类下无标签页
- 搜索无结果

建议文案：

- `当前没有可管理的标签页`
- `没有找到匹配结果`
- `试试切换分类或清空搜索条件`

## 11.2 异常状态

- 标签页读取失败
- 分类保存失败
- 批量关闭部分失败

建议反馈：

- toast 提示失败原因
- 允许重试

## 12. 权限与提示

首次使用时需提示：

- 插件会读取标签页标题和 URL 以便统一管理
- 自定义标题和备注仅保存在插件本地
- 自动分类默认基于规则，不会修改网页内容

## 13. 原型输出建议

设计阶段建议补充以下原型资产：

- 低保真线框图
- 高保真视觉稿
- 关键交互说明稿
- 组件状态表

## 14. MVP 原型优先级

P0：

- Popup 快速查看
- Manager Page 三栏布局
- 自动分类
- 手动分类
- 单个关闭 / 批量关闭
- 标签自定义标题与备注编辑

P1：

- 拖拽分类
- 高级筛选
- 最近关闭列表

P2：

- 智能推荐分类
- 保存标签组
- 恢复历史工作区
