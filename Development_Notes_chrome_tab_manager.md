# Chrome 标签页统一管理插件开发注意事项

## 1. 文档目的

本文档基于以下已有文档整理：

- `PRD_chrome_tab_manager.md`
- `Technical_Design_chrome_tab_manager.md`
- `Prototype_Spec_chrome_tab_manager.md`

目标是为研发提供一份更偏实施层的说明，聚焦以下内容：

- 开发中需要优先注意的实现约束
- 推荐的研发实施顺序
- 当前需求与技术方案中的关键风险和待确认项

---

## 2. 开发总原则

### 2.1 先做 MVP，避免功能扩张

当前版本应严格收敛在以下范围内：

- 读取并展示当前标签页
- 当前窗口 / 全部窗口查看
- 规则自动分类
- 手动分类
- 单个关闭 / 批量关闭
- 自定义标题与备注编辑
- 搜索与基础筛选

本期不应提前引入：

- AI 分类
- 云同步
- 跨设备恢复
- 团队协作
- 复杂拖拽编排
- 最近关闭恢复体系

原因：

- Manifest V3 下插件状态管理本身就有复杂度
- 提前引入扩展功能会增加存储模型和状态同步成本
- MVP 首要目标是把标签读取、分类、编辑、关闭链路跑通

### 2.2 以“真实标签状态 + 本地元数据”双层模型实现

实现时必须明确区分两类数据：

1. 浏览器真实标签数据
2. 插件本地元数据

浏览器真实标签数据来源于：

- `chrome.tabs.query`
- `chrome.tabs.get`
- `chrome.windows`

插件本地元数据仅保存：

- `customTitle`
- `note`
- `categoryId`
- `manualOverride`
- 用户偏好配置

不建议长期存储：

- 原始标签完整快照
- 高频变化的激活态、标题态、临时状态

原则：

- 页面展示时，以浏览器实时数据为准
- 插件只补充浏览器原生没有的管理信息

---

## 3. 研发中必须注意的事项

### 3.1 Manifest V3 架构限制

`background` 为 Service Worker，不应依赖内存常驻状态。

开发注意点：

- 关键状态不能只放在内存变量中
- 监听逻辑要允许 Service Worker 被回收后重新恢复
- 持久化数据必须落到 `chrome.storage.local`
- UI 页面不要假设 background 永远在线

建议：

- 标签操作统一通过 background 执行
- UI 负责展示与轻交互
- 状态刷新通过消息或重新查询实现，不依赖长生命周期连接

### 3.2 Popup 和 Manager Page 职责必须分层

Popup 应只承载高频、轻量操作：

- 当前窗口摘要查看
- 快速搜索
- 快速关闭单个标签页
- 跳转完整管理页

Manager Page 承载复杂操作：

- 全量标签管理
- 分类维护
- 批量操作
- 编辑详情

开发中不要把复杂逻辑堆进 Popup，否则会直接影响：

- 弹窗首开速度
- 状态管理复杂度
- 用户理解成本

### 3.3 手动分类必须高于自动分类

现有文档明确提到“自动分类结果可被手动覆盖”，但当前数据模型中没有清晰表达“已被手动覆盖”的字段。

建议补充字段：

```ts
type TabMeta = {
  categoryId?: string;
  customTitle?: string;
  note?: string;
  manualOverride?: boolean;
  updatedAt: number;
};
```

分类执行顺序建议固定为：

1. 用户手动指定分类
2. 按域名规则命中
3. 按标题关键词命中
4. 默认归入 `待整理`

如果没有 `manualOverride`，重新分类时极易覆盖用户操作，这是第一优先级风险。

### 3.4 `tabsMeta` 的 key 设计要先定清楚

技术文档中提到 `tabId` 或 `tabId_windowId` 可作为 key，但研发前应统一。

建议：

- 运行态映射可以用 `tabId`
- 持久化时明确清理策略，不要无限保留失效 tab 元数据

至少要先定义清楚：

- 标签关闭后是否保留元数据
- 若保留，是永久保留还是限时保留
- 保留数据是否参与“最近关闭”或未来恢复能力

如果该问题不先明确，后续会出现：

- 存储膨胀
- 已关闭标签的脏数据残留
- 新旧标签关联混乱

### 3.5 分类删除必须定义回收规则

删除分类时，不能只删分类本身，还要处理关联标签页。

建议规则：

- 删除分类后，相关标签全部转入 `待整理`
- 或转入 `未分类`

不建议直接丢失关联信息且无提示。

同时要注意：

- 系统默认分类是否允许删除
- 删除分类是否需要二次确认
- 删除后是否需要 toast 或可撤销反馈

### 3.6 批量关闭必须处理高风险和部分失败

批量关闭是高风险操作，必须设计完整反馈链路。

至少要保证：

- 执行前可确认
- 执行后返回成功数量与失败数量
- 部分失败时 UI 不要假设全成功
- 用户关闭后列表立即刷新

需要额外考虑：

- 被关闭标签中是否包含固定标签 `pinned`
- 当前激活标签关闭后的前台切换是否需要额外处理
- 用户是否开启“本次不再提示”

### 3.7 当前窗口 / 全部窗口口径必须统一

当前文档同时支持：

- 当前窗口
- 全部窗口

开发时要统一以下口径：

- 列表数据范围
- 分类统计范围
- 搜索范围
- 批量操作范围

不能出现：

- 左侧分类数量统计按全部窗口
- 中间列表却只显示当前窗口

建议把范围定义成统一查询参数：

```ts
type TabScope = "currentWindow" | "allWindows";
```

所有查询、过滤、统计都以同一个 `scope` 为准。

### 3.8 搜索、筛选、排序应作为派生状态

搜索词、排序方式、分类筛选本质上是 UI 层状态，不应直接持久化为核心业务数据。

建议：

- store 中保留原始标签数据
- 筛选条件单独存放
- 列表渲染结果通过 selector 或派生函数计算

这样可以避免：

- 状态串扰
- Popup 和 Manager 逻辑不一致
- 存储结构被临时交互污染

### 3.9 特殊页面和异常标签要兜底

Chrome 中并非所有标签都能按普通网页处理。

要考虑：

- `chrome://`
- `chrome-extension://`
- 空白页
- 无法解析 URL 的页面
- 无标题页面
- favicon 缺失

建议：

- `domain` 解析失败时提供兜底值
- favicon 缺失时显示默认图标
- 特殊页面允许展示但分类时可进入 `待整理`

### 3.10 权限和隐私文案必须与实际实现一致

文档要求：

- 读取标题和 URL 用于统一管理
- 自定义标题和备注仅本地保存
- 自动分类不修改网页内容

实现时必须保证：

- 不修改网页真实标题
- 不将备注、自定义标题隐式上传外部服务
- 若后续接 AI 分类，必须单独加开关和说明

### 3.11 性能要求要从实现阶段就控制

原型文档给出了较明确性能目标：

- Popup 打开时间小于 1 秒
- 100 个标签页以内加载流畅
- 批量操作反馈不超过 2 秒

开发中避免以下做法：

- 每次输入搜索都重新读存储
- 每次渲染都全量重新分类
- 每条标签单独查一次本地元数据
- UI 和 background 频繁重复通信

建议：

- 初始化时批量读取标签与元数据
- 一次合并，统一进入 store
- 分类结果可按触发条件更新，不要无条件重算

---

## 4. 推荐实施顺序

### 阶段 1：基础骨架

目标：

- 建立 Manifest V3 基础结构
- 打通 Popup、Manager、Background 三端入口

建议完成项：

- `manifest.json`
- `popup.html` / `manager.html`
- React + TypeScript + Vite 工程
- background service worker 基础通信
- 基础类型定义和目录结构

### 阶段 2：标签读取与统一数据模型

目标：

- 能稳定读取当前窗口 / 全部窗口标签页
- 建立“实时标签数据 + 本地元数据”合并模型

建议完成项：

- `tab-service`
- `storage-service`
- `TabEntity` / `CategoryEntity` / `Preference` 类型
- `getTabs(scope)` 查询能力
- `domain` 解析与特殊 URL 兜底

### 阶段 3：分类能力

目标：

- 跑通系统默认分类和规则分类
- 支持手动覆盖

建议完成项：

- 默认分类初始化
- 域名规则分类
- 关键词规则分类
- 手动分类覆盖逻辑
- 重新分类能力

### 阶段 4：Manager Page 核心能力

目标：

- 先完成完整管理页，再回头压缩 Popup 能力

建议完成项：

- 左侧分类栏
- 中间标签列表
- 右侧编辑区或编辑抽屉
- 新建 / 编辑 / 删除分类
- 修改标签分类、标题、备注
- 搜索、基础排序、基础筛选

### 阶段 5：标签控制与批量操作

目标：

- 打通打开、激活、关闭、批量关闭链路

建议完成项：

- `activateTab`
- `closeTab`
- `closeTabs`
- 批量选择状态
- 批量关闭确认框
- 关闭结果反馈与异常提示

### 阶段 6：Popup 快速入口优化

目标：

- 将高频能力压缩进 Popup，保证轻量

建议完成项：

- 当前窗口摘要
- 分类卡片
- 搜索框
- 单个关闭
- 跳转 Manager Page
- 重新分类快捷入口

### 阶段 7：监听与状态一致性完善

目标：

- 标签变化后 UI 能及时感知

建议完成项：

- `tabs.onCreated`
- `tabs.onUpdated`
- `tabs.onRemoved`
- `tabs.onActivated`
- 事件触发后的最小化刷新策略

---

## 5. 建议先补齐的实现规则

以下规则建议在正式编码前先固定，否则后续容易返工。

### 5.1 标签元数据结构

建议本地存储结构按“标签元数据”和“分类数据”分开：

```ts
type TabMeta = {
  categoryId?: string;
  customTitle?: string;
  note?: string;
  manualOverride?: boolean;
  createdAt: number;
  updatedAt: number;
};

type StorageSchema = {
  tabsMeta: Record<string, TabMeta>;
  categories: CategoryEntity[];
  preferences: UserPreference;
};
```

### 5.2 系统默认分类

建议 MVP 默认提供以下系统分类：

- 工作
- 学习
- 购物
- 娱乐
- 待整理

需要先定规则：

- 系统分类是否允许重命名
- 系统分类是否允许删除
- 系统分类和手动分类在 UI 中如何区分

### 5.3 标签关闭后的元数据策略

建议 MVP 先采用简单策略：

- 标签关闭后删除其元数据
- 暂不支持“最近关闭”恢复

原因：

- 可以最大限度降低存储复杂度
- 与当前 MVP 范围一致
- 后续若做恢复能力，再补充 `closed` / `saved` 状态链路

### 5.4 分类删除后的标签归属

建议统一归入：

- `待整理`

原因：

- 比“未分类”更贴近后续继续整理语义
- 与自动分类兜底逻辑一致

### 5.5 批量关闭确认策略

建议：

- 默认开启批量关闭确认
- 在确认弹层提供“本次不再提示”选项
- 该选项落到 `preferences.confirmBeforeBulkClose`

---

## 6. 风险项

### 高风险

1. 重新分类覆盖手动分类
原因：缺少 `manualOverride` 或执行优先级不明确。

2. UI 与 background 状态不一致
原因：多入口页面并存，且 MV3 service worker 非常驻。

3. 批量关闭结果不准确
原因：批量关闭后未及时刷新、未处理部分失败。

4. 持久化结构设计不稳定
原因：`tabsMeta` key、关闭后的保留策略未先定义。

### 中风险

1. Popup 逻辑过重导致性能不达标
2. 当前窗口 / 全部窗口统计口径不一致
3. 特殊页面 URL 导致分类或展示异常
4. 分类删除后关联标签处理不清晰

### 低风险但应预留

1. 后续接入 AI 分类时的权限与隐私边界
2. 标签组保存 / 恢复时的数据扩展方式
3. 云同步引入后的冲突合并策略

---

## 7. 待确认项

以下问题建议产品和研发在开发前确认：

1. Popup 默认是否只展示当前窗口，是否允许切换到全部窗口
2. Manager Page 默认展示当前窗口还是全部窗口
3. 系统默认分类是否允许删除或重命名
4. 删除分类后的标签统一进入 `待整理` 是否认可
5. 标签关闭后是否立刻清理元数据
6. 本期是否实现 `pinned` 标签的特殊保护逻辑
7. 本期是否实现“最近访问状态”字段展示
8. 搜索范围是否覆盖标题、URL、自定义标题、备注

---

## 8. 研发启动建议

建议按以下顺序启动研发：

1. 先完成工程骨架、类型定义、Manifest 和基础通信
2. 再完成标签读取、存储合并和基础列表展示
3. 然后实现分类与编辑能力
4. 再做关闭和批量操作
5. 最后压缩 Popup 交互并补监听与异常处理

简化判断标准：

- 如果某项能力会改变数据模型，优先先做
- 如果某项能力只是 UI 表现，后置
- 如果某项能力会带来高风险用户操作，必须配确认和反馈一起做

---

## 9. 结论

当前文档体系已经足够支撑 MVP 开发启动，但仍有几项实现规则需要在编码前固定，尤其是：

- 手动覆盖自动分类的模型设计
- 关闭标签后的元数据处理策略
- 当前窗口 / 全部窗口的统一口径
- 分类删除和批量关闭的风险处理

只要先把这些规则定清楚，MVP 研发可以较稳定地推进，不需要提前引入更复杂的 AI、同步或恢复能力。
